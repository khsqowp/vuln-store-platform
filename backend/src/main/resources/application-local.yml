spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vulshop?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: vulshop
    password: vulshop1234!

  jpa:
    show-sql: true

file:
  upload-dir: ./uploads

logging:
  level:
    com.vulshop: DEBUG
    org.hibernate.SQL: DEBUG
